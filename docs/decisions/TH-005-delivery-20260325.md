---
theme_id: "TH-005"
title: "リアルタイム進捗Push基盤"
phase: "delivery"
status: "awaiting-review"
source: "owner-request"
created_at: "2026-03-25"
updated_at: "2026-03-25"
next_action: "オーナーの確認後、mainへマージ"
awaiting_review: "実装内容の確認と、mainへのマージ承認"
participants:
  - "AIPO"
  - "AI PM"
  - "AI PD"
  - "AI Dev"
tags:
  - "realtime"
  - "infrastructure"
---

# Phase 6: デリバリー — 決定記録

## 日付
2026-03-25

## 議論の参加者
- AIPO, AI PM, AI PD, AI Dev

## 成果物

### 変更ファイル一覧

| ファイル | 変更内容 |
|---|---|
| `scripts/insert-discussion-log.ts` | dotenv `.env.local` 優先読み込みに修正 |
| `scripts/migrate-md-to-supabase.ts` | 同上 |
| `scripts/seed-discussion-logs.ts` | 同上 |
| `scripts/update-theme-progress.ts` | **新規** Phase進捗更新CLIスクリプト |
| `src/lib/data/themes.ts` | `updateThemeProgress()`, `validateThemeProgressInput()` 追加 |
| `src/app/page.tsx` | `revalidate` 60 → 10 |
| `src/app/themes/[themeId]/page.tsx` | `revalidate` 60 → 10 |
| `__tests__/lib/theme-progress.test.ts` | **新規** テスト16件 |

### 使い方

#### 議論ログのPush（既存・修正済み）
```bash
npx tsx scripts/insert-discussion-log.ts \
  --theme-id TH-001 \
  --agent-role "AI PM" \
  --direction response \
  --message "インサイトの分析結果をまとめました"
```

#### Phase進捗のPush（新規）
```bash
npx tsx scripts/update-theme-progress.ts \
  --theme-id TH-001 \
  --phase "implementation" \
  --status "in-progress" \
  --next-action "AI Devに実装を依頼"
```

## 受け入れ条件の検証

| # | 条件 | 結果 |
|---|---|---|
| 1 | `insert-discussion-log.ts`が`.env.local`から環境変数を読みINSERTできる | ✅ 動作確認済み |
| 2 | `update-theme-progress.ts`でPhase進捗をSupabaseに反映できる | ✅ 動作確認済み |
| 3 | revalidateが10秒に短縮され、Push後すぐに反映される | ✅ 両ページで変更済み |
| 4 | 全既存テストがパスし、ビルドが成功する | ✅ 141件パス、build成功 |

## 判断: デプロイ可否
- **デプロイ可能**
- 全受け入れ条件をクリア
- 既存機能への影響なし（revalidate短縮は改善方向の変更）

## 将来の拡張候補（今回のスコープ外）
1. **Supabase Realtime**: discussion_logsのリアルタイム表示（revalidate=10で不足する場合）
2. **Claude Code Hooks**: AIPOがBashコマンド実行を忘れた場合の自動Pushフォールバック
3. **Claude API直接呼び出し**: WebアプリからAIエージェントをキックする機能
4. **decisions内容のPush**: Phase完了時にdecisions内容もリアルタイムで反映

## 次のアクション
- オーナーの確認後、`main` へマージ
